remote_theme: pages-themes/hacker@v0.2.0
plugins:
  - jekyll-remote-theme # add this line to the plugins list if you already have one
title: Death Grunty's Homepage
description: Bookmark this to keep an eye on my project updates!
domain: evpching.github.io       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://info.deathgrunty.xyz  # the base hostname and protocol for your site, e.g. http://example.com
